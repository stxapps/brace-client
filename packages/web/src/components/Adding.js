import React from 'react';
import { connect } from 'react-redux';

class Adding extends React.Component {

  render() {
    return <div>Adding page</div>
  }
}

const mapStateToProps = null;

export default connect(mapStateToProps)(Adding);
