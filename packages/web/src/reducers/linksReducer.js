import { FETCH_LINKS, ADD_LINK, REMOVE_LINKS, DELETE_LINKS } from '../types/actions';

const initialState = {
  'My List': [],
  'Trash': [],
  'Archive': [],
};

export default (state=initialState, action) => {

  return state;
};
